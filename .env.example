# Vox Environment Variables
# Copy to .env.local and fill in your API keys

# Required
ELEVENLABS_API_KEY=your-elevenlabs-api-key
GEMINI_API_KEY=your-gemini-api-key

# Optional - OpenRouter as rewrite fallback (used if no Gemini key)
OPENROUTER_API_KEY=your-openrouter-api-key

# Optional - Deepgram Nova-3 as fallback STT provider
DEEPGRAM_API_KEY=your-deepgram-api-key

# Optional - OpenAI Whisper as fallback STT provider
OPENAI_API_KEY=your-openai-api-key

# Apple Speech is always available as a final on-device fallback (no key needed)

# Optional runtime tuning
# VOX_MAX_CONCURRENT_STT=8
# VOX_DISABLE_STREAMING_STT=1
# VOX_STT_ROUTING=hedged
# VOX_AUDIO_BACKEND=recorder
# VOX_PERF_INGEST_URL=https://your-perf-ingest-endpoint.example/ingest
